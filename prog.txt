1- el home page zay el index done *****
2- kol el pages el tanya lazem ykon logged in  done *****
3- user name w img in layout  done *****
4- facebook login ,Google login  done *****
5- add , update ,delete 
	Content ,subject , grade ,stage, lesson
6-Register Form (Desing and complete fields)
7- pop up
syle in update form
el link lazm ykono the same email sa7???????????????????????
*enum status
role id static 
Grades 
	L
	A
	E	
	U
	D
subject
	L
	A
	E	
	U
	D
lesson
	L
	A
	E	
	U
	D
user 
	L
	A
	E	
	U
	D
Roles,Permission
stage
	L
	A
	E	
	U
	D
content
	L
	A
	E	
	U
	D

session??
if user 
eslam 
	another layout
	login in layout
	login name  in layout
??el app face book need to change?

Grid Paging***
style button *** img subjects done


function requireLogin(req, res, next) {
  if (req.session.loggedIn) {
    next(); // allow the next route to run
  } else {
    // require the user to log in
    res.redirect("/login"); // or render a form, etc.
  }
}

// Automatically apply the `requireLogin` middleware to all
// routes starting with `/admin`
app.all("/admin/*", requireLogin, function(req, res, next) {
  next(); // if the middleware allowed us to get here,
          // just move on to the next route handler
});

<div class="container">

<div ng-controller="ExampleController">
  <select ng-model="template" ng-options="t.name for t in templates">
   <option value="">(blank)</option>
  </select>
  url of the template: <code>{{template.url}}</code>
  <hr/>
  <div class="slide-animate-container">
    <div class="slide-animate" ng-include="template.url"></div>
  </div>
</div>
  </div>
