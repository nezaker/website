<!DOCTYPE html>

<section id="recentlessons" >
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">أحدث الدروس</h2>
                    <h3 class="section-subheading text-muted">آخر مشاركات أعضاء نذاكر والمعلمين في قائمة الفيديوهات التالية ويمكن مشاهدة كل الفيديوهات على نذاكر بالضغط على عرض جميع الدروس</h3>
                </div>
            </div>

            <div class="row">
               <% for(var i=0; i < contents.length; i++) { 
            %>
                <div class="col-md-4 col-sm-6 portfolio-item" >
                    <a class="portfolio-link" 
                     data-toggle="modal" ng-click="SetContentID(<%=JSON.stringify(contents[i])%>)"
                     href="#portfolioModal1">
                      
                        <img class="img-fluid portfolio-img " src=<%=contents[i].thumbnail%> alt="">
                    </a>
                  <!--   <iframe class="video w100" class="img-fluid portfolio-img "  src="//www.youtube.com/embed/<%=contents[i].youtube_video_id%>" frameborder="0" allowfullscreen></iframe>
 -->
                    <div class="portfolio-caption">
                       
                        <h4><%=contents[i].lesson.subject.name%> | <%=contents[i].lesson.name%></h4>
                        <p class="text-muted"><%=contents[i].lesson.subject.grade.name%></p>
                     <p class="text-muted"><%=contents[i].user.first_name %> 
                         <%=contents[i].user.last_name%></p>

                    </div>
                </div>
            <% }%>
               
            </div>
        </div>
    </section>


  
