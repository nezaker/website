<table class="table table-striped table-hover">
  <tr class="success">
    <td>
      id
    </td>
    <td>
      name
    </td>
    <td>
      name
    </td>
  </tr>
  <tr  ng-repeat="sub in subjects" >
    <td>{{sub.id}}</td>
    <td>{{sub.name}}</td>
    <td>{{sub.grade.name}}</td>

  </tr>

</table>
